# ولا كلمة (wala-kelma)

مشروع مستقل تماماً (Next.js + Firebase) — لعبة تمثيل صامت بشاشتين متزامنتين. لا علاقة له بأي مشروع ثاني بهذا الـ workspace؛ لا تدمج ألعاب/مزايا من مشاريع أخرى فيه إلا لو طُلب صراحة.

## شاشة العرض (`/display`) مخصّصة للتلفزيون

تُفتح دائماً على تلفاز أو شاشة كبيرة يُشاهدها الناس من مسافة بعيدة (مو موبايل بيد حد). عند تعديل أي شيء بـ `app/display/page.tsx`:
- فضّل نصوص وعناصر كبيرة (استخدم `clamp()` بدل أحجام ثابتة صغيرة) — لو شي يبين صغير أو "مضغوط"، كبّره.
- استغل المساحة الفارغة بالشاشة بدل ما تخليها فاضية.
- شاشة `/host` بالمقابل هي لوحة تحكم المقدّم على جواله — تصميمها مضغوط ومقبول إنه صغير.

## أدوات إدارية مدمجة

- `app/itsadmin1/` — لوحة الإدارة (أعمال، فئات، بنر، استيراد CSV، سحب أعمال من موقع، طلبات، مستخدمون...).
- `app/api/wk-scrape` و `app/api/wk-enrich` — سحب أسماء/أنواع الأعمال وصورها من مواقع خارجية (Node runtime، cheerio) لتغذية تبويب "سحب أعمال" بالإدارة مباشرة بدون Excel.
